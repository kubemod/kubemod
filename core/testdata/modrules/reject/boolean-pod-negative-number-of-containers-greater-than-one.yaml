apiVersion: api.kubemod.io/v1beta1
kind: ModRule
metadata:
  name: modrule-1
spec:
  type: Reject

  matches:
    - query: "$.kind"
      value: "Pod"
      
    - query: "length($.spec.containers) > 1"
      negative: true
