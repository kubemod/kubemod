apiVersion: api.kubemod.io/v1beta1
kind: ModRule
metadata:
  name: modrule-1
spec:
  type: Reject

  match:
    - select: "$.kind"
      values:
        - Pod
        - Deployment
        
    - select: "$.metadata.labels.color"
      values:
        - red
        - blue
      negative: true
