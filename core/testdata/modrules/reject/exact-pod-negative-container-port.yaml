apiVersion: api.kubemod.io/v1beta1
kind: ModRule
metadata:
  name: modrule-1
spec:
  type: Reject

  matches:
    - query: "$.kind"
      value: "Pod"
      
    - query: "$.spec.containers[0].ports[0].containerPort"
      value: "80"
      negative: true
